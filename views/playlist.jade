html
    head
        meta(charset = "UTF-8")
        link(rel="stylesheet", href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css", integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh", crossorigin="anonymous")
        script(src="https://code.jquery.com/jquery-3.4.1.slim.min.js", integrity="sha384-J6qa4849blE2+poT4WnyKhv5vZF5SrPo0iEjwBvKU7imGFAV0wwj1yYfoRSJoZ+n", crossorigin="anonymous")
        script(src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js", integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo", crossorigin="anonymous")
        script(src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js", integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6", crossorigin="anonymous")
        meta(name="viewport", content="width=device-width, initial-scale=1")
        style.
            .card-img-top {width: 100%; height: 10vw; object-fit: cover;}
    body
        .container
            .row
                -for(var i = 0 ; i < result.length - 1; i++)
                    -var passwd = result[i].password;
                    -var gameTitle1 = result[i].game_name;
                    -var objectid = result[i].objectid;
                    if (i==0)
                        .col-4
                            .card(style="width: 18rem;")
                                img.card-img-top(src = "../fileStorage/" + passwd + "/" + result[i].thumbnail)
                                .card-body(align = "center")
                                    h5.card-title #{gameTitle1}
                                    a.btn.btn-primary(href='/play/:' + objectid) Play
                            br
                            br
                    else
                        -var gameTitle2 = result[i+1].game_name;
                        if(gameTitle1 != gameTitle2)
                            -var passwd = result[i+1].password;
                            -var num2 = result[i+1].number;
                            .col-4  
                                .card(style="width: 18rem;")
                                    img.card-img-top(src = "../fileStorage/" + passwd + "/" + result[i+1].thumbnail)
                                    .card-body(align = "center")
                                        h5.card-title #{gameTitle2}
                                        a.btn.btn-primary(href='/play/:' + objectid) Play
                                br
                                br
